rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // فتح القواعد تماماً للتأكد من عمل الموقع
    match /{document=**} {
      allow read, write: if true;
    }
  }
}


